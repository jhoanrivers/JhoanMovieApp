<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jhoanmovieapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HiltApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JhoanMovieApp">
        <activity
            android:name=".ui.view.DetailMovieActivity"
            android:exported="false"
            android:theme="@style/Theme.JhoanMovieApp.Detail"
            >
        </activity>
        <activity
            android:name=".ui.view.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JhoanMovieApp.TabBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>